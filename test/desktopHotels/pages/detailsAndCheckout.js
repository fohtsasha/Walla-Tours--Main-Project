const selectors = require('./selectors');
const helpers = require('../helper');
const { hotelSearch } = require('./path/to/hotelSearch');
const detailsAndCheckout = {

    }

module.exports = {
    detailsAndCheckout,
};
